import React from "react";

// Stub component because the original NewGem implementation
// is not present in this commit. Replace with real logic later if needed.
type NewGemProps = any;

const NewGem: React.FC<NewGemProps> = () => {
  return null; // no UI for now
};

export default NewGem;
