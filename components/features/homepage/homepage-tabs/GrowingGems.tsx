import React from "react";

// Stub component because the original GrowingGems implementation
// is not present in this commit. Replace with real logic later if needed.
type GrowingGemsProps = any;

const GrowingGems: React.FC<GrowingGemsProps> = () => {
  return null; // no UI for now
};

export default GrowingGems;
