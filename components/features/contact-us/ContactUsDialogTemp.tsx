import React from "react";

// Stub props type used in robot.type.ts.
// Replace this with the real props shape when you restore the original component.
export type ContactUsDialogProps = any;

// Stub component so any component imports also work.
const ContactUsDialogTemp: React.FC<ContactUsDialogProps> = () => {
  return null; // no UI for now
};

export default ContactUsDialogTemp;
